PORT=3002
CORS_ORIGIN=http://localhost:3001
PUBLIC_API_BASE=http://localhost:3002

# Comma-separated hostnames allowed through the HLS proxy (empty = allow all)
# PROXY_ALLOWLIST=devstreaming-cdn.apple.com,rthktv35-vos-live.akamaized.net

# YouTube live detection (playback always uses iframe embed)
# Default: scrape HTML when no API key is set
# YOUTUBE_DATA_API_KEY=
# YOUTUBE_LIVE_METHOD=auto
# YOUTUBE_LIVE_METHOD=scrape
# YOUTUBE_LIVE_METHOD=data_api
